import re
def extract(coord_text):
    # ========== 1. 待提取的原始坐标文本（可直接替换成你的目标文本） ==========
    # ========== 2. 核心：正则提取所有纯数字 + 赋值目标变量 ==========
    # 正则匹配所有数字（适配整数/小数，兼容任意格式的坐标文本）
    num_list = re.findall(r"\d+", coord_text)
    # 转为整数类型（坐标值默认是整数）
    num_list = [int(num) for num in num_list]

    # ========== 3. 按顺序赋值到指定变量（严格对应需求） ==========
    # 校验提取结果完整性（必须提取到8个数字，避免赋值报错）
    #if len(num_list) == 8:
    #x1, y1, x2, y2, x3, y3, x4, y4 = num_list
    #return x1, y1, x2, y2, x3, y3, x4, y4
    return num_list
        # 打印验证结果
    #     print("✅ 坐标提取成功，变量赋值完成：")
    #     print(f"x1={x1}, y1={y1}, x2={x2}, y2={y2}")
    #     print(f"x3={x3}, y3={y3}, x4={x4}, y4={y4}")
    # else:
    #     print(f"❌ 提取失败！共提取到 {len(num_list)} 个数字，需提取8个")
    #     print("🔍 请检查原始坐标文本格式是否正确！")